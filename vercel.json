{
  "build": {
    "env": {
      "JEKYLL_ENV": "production" 
    },
    "command": "bundle install && bundle exec jekyll build" 
  },
  "routes": [
    {
      "src": "/(.*)", 
      "dest": "/_site/$1" 
    }
  ]
}
